<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../../shared/includes/meta') %>
    <title>Add Roadmap Item - <%= app.name %></title>
    <%- include('../../shared/includes/styles') %>
  </head>

  <body class="bg-background">
    <div class="flex min-h-screen flex-col items-center">
      <%- include('../../shared/partials/header') %>

      <main class="w-full max-w-4xl px-4 py-8 sm:px-6 lg:px-8">
        <!-- Page Header -->
        <div class="mb-8 w-full">
          <div class="mb-2 flex items-center space-x-4">
            <a
              href="/apps/<%= app._id %>/roadmap"
              class="flex items-center text-gray-600 transition-colors hover:text-gray-900">
              <i class="fas fa-arrow-left mr-2"></i>
              Back to Roadmap
            </a>
          </div>
          <h1 class="text-gray-dark mb-2 text-3xl font-bold">
            Add Roadmap Item
          </h1>
          <p class="text-base text-gray-600">
            Create a new item for <%= app.name %>'s roadmap.
          </p>
        </div>

        <div class="rounded-2xl border border-gray-100 bg-white p-8 shadow-lg">
          <h2 class="mb-6 text-xl font-semibold text-gray-900">
            Roadmap Item Details
          </h2>
          <form
            method="POST"
            action="/apps/<%= app._id %>/roadmap/add"
            class="space-y-6">
            <!-- Basic Information -->
            <div class="space-y-4">
              <h3
                class="border-b border-gray-200 pb-2 text-lg font-medium text-gray-900">
                Basic Information
              </h3>
              <div class="grid grid-cols-1 gap-6 lg:grid-cols-2">
                <div class="lg:col-span-2">
                  <label
                    for="title"
                    class="block text-sm font-medium text-gray-700">
                    Title <span class="text-red-500">*</span>
                  </label>
                  <input
                    type="text"
                    id="title"
                    name="title"
                    required
                    maxlength="200"
                    class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none"
                    placeholder="Enter roadmap item title" />
                </div>

                <div class="lg:col-span-2">
                  <label
                    for="description"
                    class="block text-sm font-medium text-gray-700">
                    Description <span class="text-red-500">*</span>
                  </label>
                  <textarea
                    id="description"
                    name="description"
                    required
                    maxlength="2000"
                    rows="4"
                    class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none"
                    placeholder="Describe what this roadmap item involves..."></textarea>
                </div>
              </div>
            </div>

            <!-- Settings -->
            <div class="space-y-4">
              <h3
                class="border-b border-gray-200 pb-2 text-lg font-medium text-gray-900">
                Item Settings
                <span class="text-sm font-normal text-gray-500"
                  >(Status is required)</span
                >
              </h3>
              <div class="grid grid-cols-1 gap-6 lg:grid-cols-3">
                <div>
                  <label
                    for="status"
                    class="block text-sm font-medium text-gray-700">
                    Status <span class="text-red-500">*</span>
                  </label>
                  <select
                    id="status"
                    name="status"
                    required
                    class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none">
                    <option value="planned" selected>Planned</option>
                    <option value="in-progress">In Progress</option>
                    <option value="completed">Completed</option>
                    <option value="cancelled">Cancelled</option>
                  </select>
                </div>

                <div>
                  <label
                    for="priority"
                    class="block text-sm font-medium text-gray-700">
                    Priority
                  </label>
                  <select
                    id="priority"
                    name="priority"
                    class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none">
                    <option value="">Choose priority (optional)</option>
                    <option value="low">Low</option>
                    <option value="medium">Medium</option>
                    <option value="high">High</option>
                  </select>
                </div>

                <div>
                  <label
                    for="type"
                    class="block text-sm font-medium text-gray-700">
                    Type
                  </label>
                  <select
                    id="type"
                    name="type"
                    class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none">
                    <option value="">Choose type (optional)</option>
                    <option value="feature">Feature</option>
                    <option value="improvement">Improvement</option>
                    <option value="bug-fix">Bug Fix</option>
                    <option value="announcement">Announcement</option>
                  </select>
                </div>
              </div>
            </div>

            <!-- Additional Details -->
            <div class="space-y-4">
              <h3
                class="border-b border-gray-200 pb-2 text-lg font-medium text-gray-900">
                Additional Details
              </h3>

              <!-- Suggestion Link -->
              <div>
                <label
                  for="suggestion"
                  class="block text-sm font-medium text-gray-700">
                  Link to Suggestion (Optional)
                </label>
                <select
                  id="suggestion"
                  name="suggestion"
                  class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none">
                  <option value="">No linked suggestion</option>
                  <% suggestions.forEach(suggestion => { %>
                  <option value="<%= suggestion._id %>">
                    <%= suggestion.title %> <% if (suggestion.category &&
                    suggestion.category.name) { %> • <%=
                    suggestion.category.name %> <% } %> • <%=
                    suggestion.voteCount %> votes
                  </option>
                  <% }) %>
                </select>
                <p class="mt-1 text-xs text-gray-500">
                  Link this roadmap item to a specific user suggestion to show
                  the connection.
                </p>
              </div>

              <div>
                <label
                  for="estimatedReleaseDate"
                  class="block text-sm font-medium text-gray-700">
                  Estimated Release Date
                </label>
                <input
                  type="date"
                  id="estimatedReleaseDate"
                  name="estimatedReleaseDate"
                  class="mt-1 block w-full rounded-lg border border-gray-300 px-3 py-2 focus:border-blue-500 focus:ring-2 focus:ring-blue-200 focus:outline-none" />
              </div>
            </div>

            <!-- Actions -->
            <div
              class="flex justify-end space-x-4 border-t border-gray-200 pt-6">
              <a
                href="/apps/<%= app._id %>/roadmap"
                class="hover:border-gray-dark text-gray-dark flex items-center justify-center rounded-xl border-2 border-gray-300 px-6 py-3 text-center font-medium transition-all duration-200">
                Cancel
              </a>
              <button
                type="submit"
                class="bg-gray-dark hover:bg-gray-light flex cursor-pointer items-center gap-2 rounded-xl px-6 py-3 font-medium text-white">
                Create Roadmap Item
              </button>
            </div>
          </form>
        </div>
      </main>
    </div>

    <%- include('../../shared/includes/scripts') %>
  </body>
</html>
